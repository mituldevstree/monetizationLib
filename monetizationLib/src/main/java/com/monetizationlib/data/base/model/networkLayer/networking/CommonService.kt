package com.monetizationlib.data.base.model.networkLayer.networking

/**
 * Common Service represents an API Service where
 * all reasonably common endpoints are represented.
 * Endpoints which are generic and used throughout every
 * module.
 */
interface CommonService